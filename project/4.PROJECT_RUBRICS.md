# Rubric

Use this project rubric to understand and assess the project criteria.

## Structured Moderation Outputs

### Criteria

The project defines and uses a structured moderation output model

### Submission Requirements

- The ModerationResult Pydantic model includes all required fields: contains_pii, is_unfriendly, is_unprofessional, and rationale.
- All fields have the correct data types and sensible default values.
- The model is imported and used consistently by all moderation agents.
All related unit tests in tests/test_moderation_result.py pass.

## Moderation Agents

### Criteria

The project includes a functioning text moderation agent that analyzes user messages

### Submission Requirements

- The text moderation agent in agents/text_agent.py uses Gemini or a similar LLM to detect PII, unprofessional tone, and unfriendly content.
- The agent returns results as a valid ModerationResult instance.
- The model parsing and response logic work as intended.
- The implementation passes all tests in tests/test_text_agent.py.

### Criteria

The project includes an image moderation agent that processes binary inputs and returns structured results

### Submission Requirements

- The image moderation agent in agents/image_agent.py correctly wraps uploaded image bytes into a BinaryContent object.
- The agent uses Gemini or an equivalent LLM to identify disturbing or inappropriate visual content.
- The output conforms to the ModerationResult model.
- The agent passes all tests in tests/test_image_agent.py.

## Multimodal Moderation Interface

### Criteria

The project includes a Gradio Chat UI that handles multimodal input and integrates moderation

### Submission Requirements

1. The gradio_app.py implements a gr.ChatInterface supporting both text and file uploads.
2. All user messages and uploaded files are moderated before being displayed.
3. The chat interface maintains conversation state between user and customer messages.
4. All TODOs related to moderation handling are completed.
5. The implementation passes tests/test_gradio_app.py.

### Criteria

The project integrates a simulated LLM customer agent that participates in conversations

### Submission Requirements

1. The customer_agent.run(...) call is implemented asynchronously and used to generate dynamic customer responses.
2. The customer’s replies are contextually consistent with the prior conversation.
3. The conversation includes at least one flagged moderation event.
4. The simulation runs successfully through the Gradio UI.

## Observability and Tracing

### Criteria

The project includes complete tracing instrumentation with OpenTelemetry and Arize Phoenix

### Submission Requirements

- Tracing spans are implemented for "moderate_text", "chat_turn", "conversation" (with "session.id"), and "feedback".
- The spans are created using the tracer defined in tracing.py.
- Each span includes relevant attributes, such as session identifiers or feedback content.
- The traces appear correctly in the Phoenix dashboard when the app is run.

## Moderation Evals

### Criteria

The project defines and executes evaluation cases for moderation agents using Pydantic Evals

### Submission Requirements

- The evals/text/test_cases.py and evals/image/test_cases.py files include test cases covering both acceptable and unacceptable content.
- Evals use structured inputs and return results consistent with ModerationResult.
- Running evals produces meaningful results (some pass, some fail) and no runtime errors.
- The evals are implemented using Pydantic-based eval framework.

```txt

Suggestions to Make Your Project Stand Out

1. Extend moderation capabilities to include additional flags (e.g., hate speech, spam, or misinformation).

2. Add visual analytics or dashboards summarizing flagged content using backend APIs.

3. Introduce persona-based variations in the LLM customer agent to simulate different emotional tones or scenarios.
```
